apiVersion: apps/v1
kind: Deployment
metadata:
  name: limitrange-demo
  namespace: team-a-dev-quota
spec:
  replicas: 2
  selector:
    matchLabels:
      app: limitrange-demo
  template:
    metadata:
      labels:
        app: limitrange-demo
    spec:
      containers:
        - name: web
          image: ghcr.io/rummens/pluralsight-globomantics-website:v3

