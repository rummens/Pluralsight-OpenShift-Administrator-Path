# Cleanup: remove DPA and Velero stack
oc delete dpa dpa-minio -n openshift-adp

# Wait for operator to tear down Velero pods
oc delete pods -n openshift-adp -l app.kubernetes.io/name=velero --force --grace-period=0 2>/dev/null
oc delete pods -n openshift-adp -l app.kubernetes.io/name=node-agent --force --grace-period=0 2>/dev/null

# Remove velero CLI
sudo rm -f /usr/local/bin/velero
rm -f /tmp/velero.tar.gz
rm -rf /tmp/velero-v1.14.1-linux-amd64
