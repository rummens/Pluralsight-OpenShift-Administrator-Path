
# CLUSTER-SIDE VERIFICATION
oc get all -n globomantics #wait 10
oc get application globomantics-web -n openshift-gitops

# EDIT GIT — SCALE TO 4 REPLICAS
sed -i '' 's/replicas: 2/replicas: 4/' apps/globomantics-web/deployment.yaml
git commit -am "globomantics-web: scale to 4 replicas"
git push origin main #confirm

# (UI: REFRESH the tile → OutOfSync → SYNC → SYNCHRONIZE)
echo "" #confirm

# CONFIRM THE SCALE-UP LANDED
oc get deployment globomantics-web -n globomantics
oc get application globomantics-web -n openshift-gitops
