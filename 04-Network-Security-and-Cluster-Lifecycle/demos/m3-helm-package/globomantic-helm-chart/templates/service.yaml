apiVersion: v1
kind: Service
metadata:
  name: {{ include "globomantics-website.fullname" . }}
  labels:
    {{- include "globomantics-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - name: http
    port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
  selector:
    {{- include "globomantics-website.selectorLabels" . | nindent 4 }}

