apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-globomantics-website.fullname" . }}
  labels:
    {{- include "sample-globomantics-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-globomantics-website.selectorLabels" . | nindent 4 }}
