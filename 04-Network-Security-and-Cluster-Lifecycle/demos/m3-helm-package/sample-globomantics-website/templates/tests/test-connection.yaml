apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sample-globomantics-website.fullname" . }}-test-connection"
  labels:
    {{- include "sample-globomantics-website.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sample-globomantics-website.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
